<main>
  <div id="profile-app" class="profile-container">
    <!-- HEADER DO PERFIL -->
    <header class="profile-header">
      <div class="profile-id">
        <div class="avatar-wrap">
          <img class="avatar"
            src="{{#if user.avatarUrl}}{{user.avatarUrl}}{{else}}https://placehold.co/96x96/A8ED94/34A853?text={{avatarInitials}}{{/if}}"
            alt="Avatar do Usuário"
            onerror="this.onerror=null;this.src='https://placehold.co/96x96/A8ED94/34A853?text={{avatarInitials}}'">
          {{#if user.verified}}
          <span class="verified-badge" title="Perfil verificado">
            <!-- ícone check -->
            <svg viewBox="0 0 24 24" class="icon">
              <path d="M20 6L9 17l-5-5" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round"
                stroke-linejoin="round" />
            </svg>
          </span>
          {{/if}}
        </div>

        <div class="profile-texts">
          <h1 class="profile-name">{{user.name}}</h1>
          {{#if user.username}}<p class="profile-handle">@{{user.username}}</p>{{/if}}

          <!-- Barra de Progresso -->
          <div class="level-box">
            <div class="level-top">
              <span class="level-title">
                Nível {{user.level.number}}: {{user.level.title}}
              </span>
              <span class="level-next">
                Próximo Nível: {{user.level.percent}}%
              </span>
            </div>
            <div class="progress">
              <div class="progress-fill" style="width: {{user.level.percent}}%;"></div>
            </div>
          </div>
        </div>
      </div>

      <div class="profile-actions">
        <a href="/perfil/config" class="btn btn-outline">
          
          <!-- lápis -->
          <svg viewBox="0 0 24 24" class="icon icon-left">
            <path d="M3 21l3.75-.75L17.5 9.5 14.5 6.5 4.5 16.5 3 21zM14.5 6.5l3 3" fill="none" stroke="currentColor"
              stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" />
          </svg>
          Editar Perfil
        </a>
        <a href="/" class="btn btn-outline">
        <!-- Casa -->
        <svg viewBox="0 0 24 24" class="icon icon-left">
          <path d="M10 20v-6h4v6h5v-8h3L12 3 2 12h3v8z" fill="none" stroke="currentColor" stroke-width="1.8"
            stroke-linecap="round" stroke-linejoin="round" />
        </svg>
        Início
      </a>
    </header>

    <main>
      <!-- IMPACTO / LOCALIZAÇÃO -->
      <section class="impact">
        <h2 class="section-title">
          <!-- pin -->
          <svg viewBox="0 0 24 24" class="icon icon-left green">
            <path d="M12 22s7-4.5 7-11a7 7 0 1 0-14 0c0 6.5 7 11 7 11z" fill="none" stroke="currentColor"
              stroke-width="1.8" />
            <circle cx="12" cy="11" r="2.5" fill="none" stroke="currentColor" stroke-width="1.8" />
          </svg>
          Informações de Impacto
        </h2>

        <div class="chips">
          {{#if user.biome}}
          <p class="chip">
            <span class="chip-icon">🍃</span>
            Bioma Principal: <strong>{{user.biome}}</strong>
          </p>
          {{/if}}

          {{#if user.location}}
          <p class="chip">
            <span class="chip-icon">🌎</span>
            Localização Base: <strong>{{user.location}}</strong>
          </p>
          {{/if}}
        </div>
      </section>

      <!-- MÉTRICAS -->
      <section class="metrics">
        <div class="metric">
          <p class="metric-label">
            <!-- trending -->
            <svg viewBox="0 0 24 24" class="icon icon-left">
              <path d="M3 17l6-6 4 4 7-7" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round"
                stroke-linejoin="round" />
            </svg>
            Material Reciclado (Kg)
          </p>
          <p class="metric-value green"> {{metrics.earnings}} Kg</p>
          {{#if metrics.earningsDelta}}
          <p class="metric-sub green">+{{metrics.earningsDelta}} vs. mês passado</p>
          {{/if}}
        </div>

        <div class="metric">
          <p class="metric-label">
            <svg viewBox="0 0 24 24" class="icon icon-left">
              <path d="M12 17.5l-5 2.6 1-5.7L4 9.8l5.7-.8L12 3l2.3 6 5.7.8-4.1 4.6 1 5.7z" fill="none"
                stroke="currentColor" stroke-width="1.2" />
            </svg>
            Pontos Disponíveis/Todos pontos
          </p>
          <p class="metric-value">{{metrics.impactScore}}/1.495.556</p>
          <p class="metric-sub">Ações sustentáveis registradas</p>
        </div>

        <div class="metric">
          <p class="metric-label">
            <svg viewBox="0 0 24 24" class="icon icon-left">
              <path d="M4 6h16v12H4zM4 10h16" fill="none" stroke="currentColor" stroke-width="1.4"
                stroke-linecap="round" stroke-linejoin="round" />
            </svg>
            Resgates Realizados
          </p>
          <p class="metric-value">{{metrics.transactions.total}}</p>
          <div class="metric-row">
            <span class="metric-sub">90% sucesso</span>
            {{#if metrics.transactions.pending}}
            <span class="metric-sub warn">{{metrics.transactions.pending}} pendentes</span>
            {{/if}}
          </div>
        </div>
      </section>

      <!-- TABS -->
      <section class="tabs">
        <div class="tab-nav" role="tablist">
          <button class="tab-btn active" data-tab="activity" role="tab" aria-selected="true">✔ Atividade</button>
          <button class="tab-btn" data-tab="descartes" role="tab" aria-selected="false">♻️ Meus Descartes</button>
          <button class="tab-btn" data-tab="rewards" role="tab" aria-selected="false">🏆 Recompensas & Badges</button>
          <button class="tab-btn" data-tab="connections" role="tab" aria-selected="false">👥 Conexões</button>
        </div>

        <!-- Atividade -->
        <div id="content-activity" class="tab-panel">
          <h2 class="panel-title">Histórico de Resgates</h2>
          <ul class="activity-list">
            {{#if activities.length}}
            {{#each activities}}
            <li class="activity-item">
              <div class="activity-icon {{iconBg}}">{{icon}}</div>
              <div class="activity-body">
                <p class="activity-title">{{this.title}}</p>
                <p class="activity-sub">{{this.subtitle}}</p>
              </div>
              {{#if this.amount}}
              <span class="activity-amount {{this.amountClass}}">{{this.amount}}</span>
              {{/if}}
            </li>
            {{/each}}
            {{else}}
            <li class="empty">Nenhuma atividade recente.</li>
            {{/if}}
          </ul>
        </div>

        <!-- Histórico de Descartes -->
        <div id="content-descartes" class="tab-panel hidden">
          <h2 class="panel-title">♻️ Histórico de Descartes</h2>
          <p class="text-sm text-gray-600 mb-4">Todos os materiais que você descartou e os pontos ganhos</p>
          
          {{#if historico.length}}
          <div class="space-y-3">
            {{#each historico}}
            <div class="bg-white border border-gray-200 rounded-lg p-4 hover:shadow-md transition">
              <div class="flex justify-between items-start">
                <div class="flex-1">
                  <div class="flex items-center gap-2 mb-1">
                    <span class="text-lg">♻️</span>
                    <h3 class="font-semibold text-gray-800">{{this.material}}</h3>
                    <span class="px-2 py-0.5 bg-green-100 text-green-700 text-xs rounded-full font-medium">{{this.categoria}}</span>
                  </div>
                  <p class="text-sm text-gray-600 mb-2">
                    <strong>{{this.quantidade}} {{this.unidade}}</strong> descartado(s)
                  </p>
                  <div class="flex items-center gap-2 text-xs text-gray-500">
                    <span>📍 {{this.pontoColeta}}</span>
                  </div>
                  <p class="text-xs text-gray-400 mt-1">{{this.endereco}}</p>
                </div>
                <div class="text-right">
                  <div class="flex items-center gap-1 mb-1">
                    <span class="text-2xl font-bold text-green-600">+{{this.pontos}}</span>
                    <span class="text-sm text-gray-500">pts</span>
                  </div>
                  <p class="text-xs text-gray-500">{{this.data}} às {{this.hora}}</p>
                </div>
              </div>
            </div>
            {{/each}}
          </div>
          
          <div class="mt-6 p-4 bg-green-50 border border-green-200 rounded-lg">
            <div class="flex items-center justify-between">
              <div>
                <p class="text-sm text-gray-600">Total de Pontos Acumulados</p>
                <p class="text-3xl font-bold text-green-700">{{user.pontos}}</p>
              </div>
              <div class="text-5xl">🌱</div>
            </div>
          </div>
          {{else}}
          <div class="text-center py-12">
            <div class="text-6xl mb-4">♻️</div>
            <p class="text-gray-500 mb-2">Você ainda não tem descartes registrados</p>
            <p class="text-sm text-gray-400">Visite um ponto de coleta e comece a acumular pontos!</p>
          </div>
          {{/if}}
        </div>

        <!-- Recompensas -->
        <div id="content-rewards" class="tab-panel hidden">
          <h2 class="panel-title">Seus Badges de Sustentabilidade</h2>

          <div class="badges-grid">
            {{#if badges.length}}
            {{#each badges}}
            <div class="badge-card {{#if this.locked}}is-locked{{/if}}">
              <span class="badge-emoji" title="{{this.title}}">{{this.emoji}}</span>
              <p class="badge-title">{{this.title}}</p>
              <p class="badge-sub">{{this.description}}</p>
            </div>
            {{/each}}
            {{else}}
            <p class="empty">Você ainda não possui badges.</p>
            {{/if}}
          </div>

          {{#if badgesNext}}
          <div class="next-goal">
            <p>Seu próximo objetivo é o badge <strong>"{{badgesNext.title}}"</strong> — {{badgesNext.hint}}.</p>
          </div>
          {{/if}}
        </div>

        <!-- Carteira -->
        <div id="content-wallet" class="tab-panel hidden">
          <h2 class="panel-title">Visão Geral da Carteira</h2>
          <p class="panel-placeholder">Aqui você veria seus saldos, alocações de ativos e histórico detalhado de
            investimentos.</p>
        </div>

        <!-- Conexões -->
        <div id="content-connections" class="tab-panel hidden">
          <h2 class="panel-title">Sua Rede Greendrop</h2>
          <p class="panel-placeholder">Aqui você veria suas conexões e rede de parceiros.</p>
        </div>
      </section>
  </div>
</main>